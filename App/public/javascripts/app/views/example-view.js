var App = App || {};

App.Views = App.Views || {};

(function($) {
	'use strict';

	App.Views.ExampleView = Backbone.View.extend({

		'events': {	},

		'initialize': function() {

			
		}

	});
}(jQuery));